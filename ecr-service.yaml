apiVersion: v1
kind: Service
metadata:
  name: ecr-formui-service
  namespace: ecr-demo

spec:
  type: NodePort

  selector:
    app: ecr-formui

  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30081

